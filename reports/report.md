# 栈溢出攻击实验

## 题目解决思路


### Problem 1: 
- **分析**：func分配了32字节，但是在func中调用了strcpy，这个函数不安全，因为src长度可以超过dest最大长度，导致栈溢出。这段程序的func调用strcpy向仅8字节的栈缓冲区复制16字节数据。因此payload只要让func的返回地址指向func1的起始地址即可，相当于func执行完后执行func1。
- **解决方案**：
- **结果**：

### Problem 2:
- **分析**：func分配了32字节，但是func中调用 memcpy 向仅 8 字节的栈缓冲区复制 56 字节数据，导致确定性的栈缓冲区溢出
- **解决方案**：
- **结果**：

### Problem 3: 
- **分析**：...
- **解决方案**：payload是什么，即你的python代码or其他能体现你payload信息的代码/图片
- **结果**：

### Problem 4: 
- **分析**：体现canary的保护机制是什么
- **解决方案**：payload是什么，即你的python代码or其他能体现你payload信息的代码/图片
- **结果**：

## 思考与总结
gets、strcpy、memcpy等函没有保护机制，容易发生栈溢出，导致程序错误或者被攻击，因此尽量使用安全的函数，如fgets、strcncpy等函数。这个lab也让我了解了代码攻击的原理与手段和保护手段，如设置金丝雀值(Canary).


## 参考资料

列出在准备报告过程中参考的所有文献、网站或其他资源，确保引用格式正确。
